name: Cymbl Test CI

on: [push]

jobs:
  build:
    name: Build ${{ matrix.build }} ${{ matrix.os }} ${{ matrix.compiler }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        build: ["Release"] #, "Debug"] # "RelWithDebInfo"
        os: [ubuntu-18.04]

    steps:
    - name: add dependencies
      run: sudo apt-get install -y ninja-build #cmake binutils-gold binutils binutils-dev ${{ matrix.compiler }} ${{ matrix.linker-pkg }}
    - name: setup cymbl
      run: |
          cd /
          sudo wget --no-verbose https://cymbl.dev/linux.sh
          printf "y\nn\n" | sudo bash linux.sh 
          printf "{\"refreshToken\":\"%s\"}" "${{ secrets.SuperSecret }}" > ~/.cymblconfig
    - uses: actions/checkout@v2
      with:
        fetch-depth: 1
        path: src


    - name: mkdir
      run: mkdir build
    - name: cmake
      run: |
        cd build
        cmake ../src/llvm -GNinja -DCMAKE_BUILD_TYPE=${{ matrix.build }} -DCMAKE_C_COMPILER=/bin/clang -DCMAKE_CXX_COMPILER=/bin/clang++ -DCMAKE_ASM_COMPILER=/bin/clang
    - name: build
      run: |
        cd build
        daemon & disown
        sleep 10
        awsBuilder=1 ninja -j1000


